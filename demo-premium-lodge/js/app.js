// === INIT ===
document.addEventListener('DOMContentLoaded', () => {
  initLanguage();
  initNav();
  initStickyBar();
  initScrollAnimations();
  initScrollTop();
  initBooking();
  initModal();
  initBeatBookLinks();
  initBeatFromURL();
  initStatsCounter();
});
