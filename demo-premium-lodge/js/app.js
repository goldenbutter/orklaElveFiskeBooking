// === INIT ===
document.addEventListener('DOMContentLoaded', () => {
  initLanguage();
  initNav();
  initScrollAnimations();
  initScrollTop();
  initBooking();
  initModal();
  initBeatBookLinks();
  initBeatFromURL();
  initStatsCounter();
});
